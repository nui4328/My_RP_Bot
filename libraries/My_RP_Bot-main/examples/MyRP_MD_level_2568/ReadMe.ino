void read_me()
    {
       servo28_Place();  //---->> ปล่อยกล่อง 
       servo29_Place();  //---->> ปล่อยกล่อง 
      fw(50, 50, 1.5, 30, "line");        //----->> 50, 50คือความรเร็วม,  1.5คือค่า kp ของสมการ pid  30คือระยะทางมีหน่วยเป็นเซนติเมตรม,  "line"คือ เคลื่อนเข้าหาเส้นเมื่อถึงระยะที่กำหนด 
      fw(50, 50, 1.5, 30, "none_line");   //----->> 50, 50คือความรเร็วม,  1.5คือค่า kp ของสมการ pid  30คือระยะทางมีหน่วยเป็นเซนติเมตรม,  "none_line"คือ ไม่เคลื่อนเข้าหาเส้นเมื่อถึงระยะที่กำหนด

      fw(50, 50, 1.5, 30, "line", 300);        //----->> 50, 50คือความรเร็วม, 1.5คือค่า kp ของสมการ pid  30คือระยะทางมีหน่วยเป็นเซนติเมตร,  "line"คือ เคลื่อนเข้าหาเส้นเมื่อถึงระยะที่กำหนด  300คือเลื่อนแขนขึ้นหน่วงเวลา 300
      fw(50, 50, 1.5, 30, "none_line", 300);   //----->> 50, 50คือความรเร็วม,  1.5คือค่า kp ของสมการ pid  30คือระยะทางมีหน่วยเป็นเซนติเมตรม,  "none_line"คือ ไม่เคลื่อนเข้าหาเส้นเมื่อถึงระยะที่กำหนด 300คือเลื่อนแขนขึ้นหน่วงเวลา 300
      
      bw(50, 50, 1.5, 30, "line");        //----->> 50, 50คือความรเร็วม,  1.5คือค่า kp ของสมการ pid 30คือระยะทางมีหน่วยเป็นเซนติเมตรม,  "line"คือ เคลื่อนเข้าหาเส้นเมื่อถึงระยะที่กำหนด 
      bw(50, 50, 1.5, 30, "none_line");   //----->> 50, 50คือความรเร็วม,  1.5คือค่า kp ของสมการ pid 30คือระยะทางมีหน่วยเป็นเซนติเมตรม,  "none_line"คือ ไม่เคลื่อนเข้าหาเส้นเมื่อถึงระยะที่กำหนด

      bw(50, 50, 1.5, 30, "line", 300);        //----->> 50, 50คือความรเร็วม, 1.5คือค่า kp ของสมการ pid   30คือระยะทางมีหน่วยเป็นเซนติเมตรม,  "line"คือ เคลื่อนเข้าหาเส้นเมื่อถึงระยะที่กำหนด  300คือเลื่อนแขนขึ้นหน่วงเวลา 300
      bw(50, 50, 1.5, 30, "none_line", 300);   //----->> 50, 50คือความรเร็วม,  1.5คือค่า kp ของสมการ pid  30คือระยะทางมีหน่วยเป็นเซนติเมตรม,  "none_line"คือ ไม่เคลื่อนเข้าหาเส้นเมื่อถึงระยะที่กำหนด 300คือเลื่อนแขนขึ้นหน่วงเวลา 300

      fw_chopsticks (20, 20, 1.5, 30, "line");  //---->> เดินหน้าขึ้นตะเกียบ  เหมือนกับ fw_line
      fw_chopsticks (20, 20, 1.5, 30, "none_line");

      bw_chopsticks (20, 20, 1.5, 30, "line");  //---->> ถอยหลังขึ้นตะเกียบ  เหมือนกับ fw_line
      bw_chopsticks (20, 20, 1.5, 30, "none_line");

      fw_bridge(50, 50, 1.5, 30, "line");    //---->> ขึ้นสะพาน  เหมือนกับ fw_line
      fw_bridge(50, 50, 1.5, 30, "none_line");

      fw_distance(10, 10, 1.5, 2700, 1000) ;   //-->> 10, 10คือความรเร็วม, 1.5คือค่า kp  2700คือค่าการวัดระยะ  1000คือค่าการไสลขึ้นของแขน
      

      moveLR(70, 90);   //------>> หมุนตามเข็มนาฬิกา  50คือความเร็วในการหมุน  90คือองศาในการหมุน
      moveLR(70, -90);  //------>> หมุนทวนเข็มนาฬิกา
      
      moveLR(150, 70, -90);   //------>> หมุนตามเข็มนาฬิกา  150คือ เดินหน้าหรือถอยหลังออกมาจากเส้นเป็นระยที่กำหนดก่อนหมุน 50คือความเร็วในการหมุน  90คือองศาในการหมุน
      moveLR(150, 70, -90);    

      set_f(2);  
      set_b(2);
          
                                //--------->>> คำสั่งรอปุ่มกด
      bz(100);                        //--------->>> คำสั่งเปิดเสียง        
      Motor(30, 30);                  //--------->>> คำสั่งควบคุมการทำงานของมอเตอร์
      Motor(-30, -30); 
      
      mcp_f(0);                       //--------->>> คำสั่งแสดงค่าของ sensor แผงหน้าที่อ่านได้ ขณะนั้น mcp_f(0 - 7);
      md_mcp_f(0);                    //--------->>> คำสั่งแสดงค่ากลางของ sensor  md_mcp_f(0 - 7);
      max_mcp_f(0);                   //--------->>> คำสั่งแสดงค่าสูงสุดของ sensor  max_mcp_f(0 - 7);
      min_mcp_f(0);                   //--------->>> คำสั่งแสดงค่าต่ำสุดของ sensor  min_mcp_f(0 - 7);

     delay(500);
         
    }
