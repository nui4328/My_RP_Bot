void readme()
  {
      servo(27, 90);        //------>> ควบคุม servo 27คือขาpinที่เสียบมี 27, 28, 8 /90คือองศา
      arm_ready();          //------>> กางมือออกและเอาแขนลง
      arm_slow_down();      //------->> เอาแขนลงช้า ๆ 
      arm_close_up();       //------->> หุบมือเข้า เอาแขนขึ้น
      arm_open_up();        //------->> กางมือออก เอาแขนขึ้น
      arm_down_open_up();   //------->> เอาแขนลง กางมือออก ยกมือขึ้น

      Motor(30, 30);       //------>> ควบคุมการหมุนของมอเตอร์ ค่าเต็มบวกคือ เดินหน้า
      Motor(-30, -30);       //------>> ควบคุมการหมุนของมอเตอร์ ค่าเต็มลบคือ เดินถอยหลัง

      fw_line(30, 30, 0.25, 's', "a5", 20);//---->> ทางตรงค่า pid = 0.25 แยกอยู่ทางขวา หยุด
      fw_line(30, 30, 0.25, 's', "a0", 20);//---->> ทางตรงค่า pid = 0.25 แยกอยู่ทางซ้าย หยุด

      fw_line(30, 30, 0.85, 's', "a5", 20);//---->> ทางโค้งค่า pid = 0.85 แยกอยู่ทางขวา หยุด ค่า 0.85 เพิ่มลดได้ตามความเหมาะสม
      fw_line(30, 30, 0.85, 's', "a0", 20);//---->> ทางโค้งค่า pid = 0.85 แยกอยู่ทางซ้าย หยุด
      
      fw_line(30, 30, 0.25, 'p', "a5", 1);//---->> ทางตรงค่า pid = 0.25 แยกอยู่ทางขวา ข้าม
      fw_line(30, 30, 0.25, 'p', "a0", 1);//---->> ทางตรงค่า pid = 0.25 แยกอยู่ทางซ้าย ข้าม

      fw_line(30, 30, 0.85, 'p', "a5", 1);//---->> ทางโค้งค่า pid = 0.85 แยกอยู่ทางขวา ข้าม
      fw_line(30, 30, 0.85, 'p', "a0", 1);//---->> ทางโค้งค่า pid = 0.85 แยกอยู่ทางซ้าย ข้าม

      fw_mission(15, 15, 0.55, 80, 10); //------->>  ใช้เดินเข้าหากระป๋องเมื่อเจอพื้นขาว จะหยุดถึงแม้ระยะทางยังไม่หมด
                                        //------->> 15, 15,คือความเร็ว  0.55คือค่าkp  80คือระยะทาง  10คือค่าoffsetเอาไว้สำหรับหยุด

      fw_distance(20, 20, 0.70, 50, 10);    //---->> เดินหน้าแบบกำหนดระยะทาง 20, 20,คือความเร็ว  0.70คือค่าkp  5คือระยะทาง  10คือค่าoffsetเอาไว้สำหรับหยุด
      fw_distance(20, 20, 50, 10);    //---->> เดินหน้าแบบกำหนดระยะทาง 20, 20,คือความเร็ว  0.70คือค่าkp  5คือระยะทาง  10คือค่าoffsetเอาไว้สำหรับหยุด
      //------------------------------------------------------\\
      bw_distance(30, 30, 0.85, 5, 2);    //---->> เดินถอยหลังแบบกำหนดระยะทาง 30, 30,คือความเร็ว  0.85คือค่าkp  5คือระยะทาง  2คือค่าoffsetเอาไว้สำหรับหยุด
      bw_distance(30, 30, 10, 10);        //---->>  ใช้ในกรณีไม่ใส่เซนเซอร์หลัง 
      //------------------------------------------------------\\
      
      turn_left_sensor(3, 34, "a2", 30);  //---->> เซนเซอร์หน้าแตะเส้นหมุนซ้าย 3คือระยะทางที่เดินต่อ 34คือความเร็วในการหมุน "a2"คือใช้เซนเซอร์a2แตะเส้นหยุด 
                                          //---->> 30คือค่าoffsetเอาไว้สำหรับหยุด
      turn_left_sensor(3, 34, 45, 30);    //---->> เซนเซอร์หน้าแตะเส้นหมุนซ้าย 3คือระยะทางที่เดินต่อ 34คือความเร็วในการหมุน 45คือองศาในการหยุด 
                                          //---->> 30คือค่าoffsetเอาไว้สำหรับหยุด

      turn_right_sensor(3, 34, "a3", 30);  //---->> เซนเซอร์หน้าแตะเส้นหมุนขวา 3คือระยะทางที่เดินต่อ 34คือความเร็วในการหมุน "a2"คือใช้เซนเซอร์a2แตะเส้นหยุด 
                                          //---->> 30คือค่าoffsetเอาไว้สำหรับหยุด
      turn_right_sensor(3, 34, 45, 30);   //---->> เซนเซอร์หน้าแตะเส้นหมุนขวา 3คือระยะทางที่เดินต่อ 34คือความเร็วในการหมุน 45คือองศาในการหยุด 
                                          //---->> 30คือค่าoffsetเอาไว้สำหรับหยุด

      turn_left_wheel(8, 34, "a2", 30);   //---->> ล้อแตะเส้นหมุนซ้าย 3คือระยะทางที่เดินต่อ 34คือความเร็วในการหมุน "a2"คือใช้เซนเซอร์a2แตะเส้นหยุด 
                                          //---->> 30คือค่าoffsetเอาไว้สำหรับหยุด
      turn_left_wheel(8, 34, 45, 30);     //---->> ล้อหน้าแตะเส้นหมุนซ้าย 3คือระยะทางที่เดินต่อ 34คือความเร็วในการหมุน 45คือองศาในการหยุด 
                                          //---->> 30คือค่าoffsetเอาไว้สำหรับหยุด

      turn_right_wheel(8, 34, "a3", 30);  //---->> ล้อแตะเส้นหมุนขวา 3คือระยะทางที่เดินต่อ 34คือความเร็วในการหมุน "a2"คือใช้เซนเซอร์a2แตะเส้นหยุด 
                                          //---->> 30คือค่าoffsetเอาไว้สำหรับหยุด
      turn_right_wheel(5, 34, 45, 30);    //---->> ล้อแตะเส้นหมุนขวา 3คือระยะทางที่เดินต่อ 34คือความเร็วในการหมุน 45คือองศาในการหยุด 
                                          //---->> 30คือค่าoffsetเอาไว้สำหรับหยุด
      turn_gyro_sensor(2, 70, 30, 20) ;
      turn_gyro_wheel(2, 44, 30, 30);
      read_sensor(0)    ;   
      md_sensor(0)      ;

      Motor(30, 30);
  }
